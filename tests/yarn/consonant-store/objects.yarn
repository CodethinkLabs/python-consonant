Fetching objects from a store using consonant.store
===================================================

Fetch objects from a non-existent commit in a store using consonant.store
-------------------------------------------------------------------------

    SCENARIO fetch objects from a non-existent commit in a store using consonant.store
    GIVEN    the standard test store "consonant-test-store-01"
    WHEN     fetching objects from the non-existent commit 12345678
    THEN     a CommitNotFoundError is thrown
